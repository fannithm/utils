{
  "name": "@fannithm/utils",
  "version": "0.1.0-snapshot.1",
  "main": "dist/index.js",
  "license": "AGPL-3.0",
  "scripts": {
    "clean": "del-cli -f coverage dist .nyc_output",
    "test": "ava",
    "build": "tsc",
    "build:all": "yarn clean & yarn build & yarn build:coverage",
    "build:coverage": "nyc ava",
    "watch:test": "ava -w",
    "watch:build": "tsc -w"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/fannithm/lib.git"
  },
  "devDependencies": {
    "@istanbuljs/nyc-config-typescript": "^1.0.1",
    "ava": "^3.15.0",
    "del-cli": "^4.0.1",
    "nyc": "^15.1.0",
    "typescript": "^4.3.5"
  }
}
